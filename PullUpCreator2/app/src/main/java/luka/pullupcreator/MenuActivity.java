package luka.pullupcreator;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.ListView;

import java.util.ArrayList;
/*
public class MenuActivity extends AppCompatActivity {

     ListView lvSet;
    SettingsAdapter mSettingsAdapter;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_menu);
        this.setUpUI();
    }

    private void setUpUI() {
        this.lvSet = (ListView) this.findViewById(R.id.lvSet);
        this.mSettingsAdapter = new SettingsAdapter(this.loadSettings());
        this.lvSet.setAdapter(this.mSettingsAdapter);
    }

    private ArrayList<Settings> loadSettings() {
        ArrayList<Settings> item = new ArrayList<>();
        books.add(new Settings("The Hobbit", "J.R.R. Tolkien", "30 sek"));

        return books;
    }
}
*/